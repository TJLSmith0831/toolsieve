# toolsieve

Standalone open-source MCP server that aggregates real downstream MCP servers and routes tool selection via semantic embedding matching, proxies calls through, and reports live token savings.
