# Routing benchmark results

On a catalog of 181 tools from 25 real MCP servers, toolsieve resolves a tool lookup in **~558 tokens against the 14,418 it would cost to load the catalog into context**. The right tool arrives callable **69%** of the time and is at least *named* **95%** of the time, so a client that misses does one cheap exact-name lookup instead of guessing what else might exist.

On queries that share no vocabulary with the tool's own description — the `paraphrase` tier, and the case lexical search is worst at — it finds the tool **51%** of the time against BM25's **32%**.

Against toolsieve's own v0.2 shape — same embeddings, same ranking, only the response redesigned — resolving a lookup costs **1% less** at live public MCP servers, **30% less** at GitHub-heavy catalog, and 57% *more* on this catalog's unusually small schemas. The odds the right tool is visible at all rise from **85% to 95%**, and it is failed lookups, not successful ones, that dominate the bill.

> **Two honest caveats.** (1) This catalog averages 79 tokens per tool; live public MCP servers measure ~154 and a GitHub-heavy set ~269, and the saving *grows* with schema size, so these rows are the conservative end — see Schema-size sensitivity. (2) Loading a catalog is a one-off while routing is charged per lookup — toolsieve is ahead for the first **26 lookups** of a session at this catalog size, after which the up-front load would have been cheaper on tokens alone (though not on context window, nor on selection accuracy past ~30-50 tools).

Generated by `benchmarks/run_benchmark.py`. Re-run it after any material change to the catalog or the router — these numbers are quoted in the README.

## Setup

- **Catalog**: 181 tools across 25 real MCP servers (`catalog.json`)
- **Queries**: 159 with ground-truth expected tools, tagged exact / paraphrase / ambiguous (`queries.jsonl`)
- **Tokenizer**: `tiktoken/cl100k_base` — a real tokenizer, not toolsieve's runtime chars/4 estimate
- **Embedding model**: `BAAI/bge-small-en-v1.5` (toolsieve's shipped default)
- **k**: 3 for the ranked methods.

### Reading the columns

- **schemas** — tools handed over with a full input schema, i.e. callable without a further lookup. This is where the tokens go: a schema is roughly ten times a bare name.
- **callable** — the right tool arrived with its schema. The task proceeds on this call; nothing more is spent on discovery.
- **visible** — the right tool was at least *named* in the response. Worst case one exact-name lookup follows, which is cheap and deterministic. The gap between `callable` and `visible` is the share of queries rescued from a blind reformulation — the failure that made v0.2 cost four searches to make three calls. For methods that only return schemas, the two columns are identical by construction.
- **tokens/call** — one response, counted whole: schemas, descriptions and roster alike. **Do not read this as the headline.** A shape that answers cheaply but misses often looks best here and is worst in the next column.
- **calls** — expected `find_tools` calls to actually resolve a query: 1 when the schema arrived, 2 when only the name did, 4 when neither (the rate observed in the pre-release smoke test).
- **tokens/resolved** — `tokens/call` x `calls`. **This is the column to compare shapes on**, and the one that reverses the apparent verdict against toolsieve's own v0.2.

## Results

| catalog | method | schemas | callable | visible | exact | paraphrase | ambiguous | tokens/call | calls | tokens/resolved |
|---|---|---|---|---|---|---|---|---|---|---|
| 10 | naive (no routing) | 10 | 1.00 | 1.00 | 1.00 | 1.00 | 1.00 | 834 | 1.00 | 834 |
| 10 | BM25 (`rank-bm25`) | 3 | 1.00 | 1.00 | 1.00 | 1.00 | 1.00 | 290 | 1.00 | 290 |
| 10 | toolsieve v0.2 | 3 | 1.00 | 1.00 | 1.00 | 1.00 | 1.00 | 278 | 1.00 | 278 |
| 10 | **toolsieve** | 1 | 1.00 | 1.00 | 1.00 | 1.00 | 1.00 | 330 | 1.00 | 330 |
| 25 | naive (no routing) | 25 | 1.00 | 1.00 | 1.00 | 1.00 | 1.00 | 2,018 | 1.00 | 2,018 |
| 25 | BM25 (`rank-bm25`) | 3 | 0.79 | 0.79 | 1.00 | 0.55 | 0.73 | 250 | 1.63 | 409 |
| 25 | toolsieve v0.2 | 3 | 1.00 | 1.00 | 1.00 | 1.00 | 1.00 | 248 | 1.00 | 248 |
| 25 | **toolsieve** | 1 | 0.95 | 1.00 | 1.00 | 1.00 | 0.82 | 449 | 1.05 | 473 |
| 50 | naive (no routing) | 50 | 1.00 | 1.00 | 1.00 | 1.00 | 1.00 | 4,010 | 1.00 | 4,010 |
| 50 | BM25 (`rank-bm25`) | 3 | 0.76 | 0.76 | 1.00 | 0.47 | 0.79 | 250 | 1.71 | 428 |
| 50 | toolsieve v0.2 | 3 | 0.98 | 0.98 | 1.00 | 0.95 | 1.00 | 241 | 1.05 | 254 |
| 50 | **toolsieve** | 1 | 0.84 | 1.00 | 1.00 | 0.74 | 0.71 | 423 | 1.16 | 492 |
| 100 | naive (no routing) | 100 | 1.00 | 1.00 | 1.00 | 1.00 | 1.00 | 8,261 | 1.00 | 8,261 |
| 100 | BM25 (`rank-bm25`) | 3 | 0.77 | 0.77 | 1.00 | 0.45 | 0.86 | 259 | 1.69 | 437 |
| 100 | toolsieve v0.2 | 3 | 0.92 | 0.92 | 1.00 | 0.79 | 1.00 | 254 | 1.23 | 312 |
| 100 | **toolsieve** | 1 | 0.76 | 0.99 | 1.00 | 0.58 | 0.64 | 410 | 1.26 | 517 |
| 181 | naive (no routing) | 181 | 1.00 | 1.00 | 1.00 | 1.00 | 1.00 | 14,418 | 1.00 | 14,418 |
| 181 | BM25 (`rank-bm25`) | 3 | 0.65 | 0.65 | 1.00 | 0.32 | 0.78 | 240 | 2.04 | 489 |
| 181 | toolsieve v0.2 | 3 | 0.85 | 0.85 | 1.00 | 0.70 | 0.91 | 244 | 1.45 | 355 |
| 181 | **toolsieve** | 1 | 0.69 | 0.95 | 1.00 | 0.51 | 0.56 | 396 | 1.41 | 558 |

## Schema-size sensitivity

This catalog's schemas average **80 tokens per tool**. Live public MCP servers measure **154** (`server-everything` + `filesystem` + `memory` + `sequential-thinking`, dumped over stdio) and a GitHub-heavy set measures **269**. That gap matters more than it looks: a response shape is mostly a bet on how much a schema costs, so the same table can rank two shapes differently at different sizes. Below, only the schema term is re-priced — descriptions, names and roster stay exactly as measured, and recall is untouched because the router ranks on name + description and never reads a schema.

Tokens per **resolved** query, 181-tool catalog:

| method | 80 tok/tool (this catalog) | 154 tok/tool (live public MCP servers) | 269 tok/tool (GitHub-heavy catalog) |
|---|---|---|---|
| naive (no routing) | 14,418 | 27,874 | 48,689 |
| BM25 (`rank-bm25`) | 489 | 941 | 1,644 |
| toolsieve v0.2 | 355 | 671 | 1,172 |
| **toolsieve** | 558 | 661 | 823 |

The two shapes cross at **~151 tokens per tool**. Below that, shipping three schemas and no roster is cheaper; above it, shipping one schema and a roster of names is — and every real catalog measured here sits above it. The benchmark catalog is the exception, not the case to optimise for.

### What this table cannot see

Every query in the set has a correct answer. The case that actually cost the most in the pre-release smoke test does not: the client was hunting a `get_repository` that **does not exist** on the GitHub MCP server, and no ranking can retrieve a tool that is not there. A ranked list has no way to say so, so the client rewords and searches again; a roster of names does, and that difference is invisible here because it never arises.

So treat these rows as the *conservative* estimate of the roster's value, and the live A/B as the measurement of it: against real GitHub and Context7 servers the same task went from 4 `find_tools` calls to 3 with no variance across three runs, and `find_tools` token cost fell 62%.

## How to reproduce

```sh
uv sync --group bench
uv run --group bench python benchmarks/run_benchmark.py
uv run python benchmarks/render_results.py
```
